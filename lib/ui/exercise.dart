import 'package:flutter/material.dart';

class exercise extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text('ออกกำลังกาย'),centerTitle: true,)
    );
  }

}